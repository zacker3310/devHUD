# Verify: devHUD v1

TASK: T1   **claims:** .buildloop/claims/T1.md   **date:** 2026-09-04
**RESULT: PASS** (with accepted gaps, listed below)

## 5a Self-check
| Check | Result |
|---|---|
| xcodegen generate | PASS |
| xcodebuild build | PASS, no warnings in App/ |
| xcodebuild test | PASS, 32 tests, 0 failures |
| live screenshot (floating panel, both cards) | PASS, three screenshots across the session |
| test listener on :4173 appears as a row | PASS ("scratchpad  localhost:4173 Python") |
| ~/.portless absent | PASS, no error, Dev Servers populated |
| RSS after 25 s | 93.8 MB, CPU 0.6 percent, zero stray lsof processes |
| CPU time unit conversion | 1.000 s measured for 1 s burn |

## 5b Evals
Not applicable: no agent configuration changed except the new project CLAUDE.md
(canary check passes).

## 5c Formal verification
ORDERING_SURFACE is concurrency but every writer is on the main actor and the
only cross-task state is `previousCPU` and `states`, both guarded after
remediation (isSampling, re-read after await, cancellation ignored). Triaged
out of TLA+; tla-gate.json has no checks.

## 5d Cross-model doubt
Two reviewers. `codex exec` failed on its configured model (needs a newer CLI)
and ran with `-m gpt-5.5`: 16 findings. An Opus code-reviewer subagent, which
also probed libproc on this machine and re-ran the tests: 20 findings.

### Fixed
- Codex provider loaded whole rollout files (239 MB, 1 GB peak RSS every 120 s). Now tail-reads 4 MB.
- AIUsageStore read-modify-write across the await; refreshNow charged a failure per provider. Re-reads after await, ignores cancellation.
- PortsStore reentrancy between the loop and manual refresh; main-thread libproc and package.json reads. isSampling guard, enrichment in the detached task, CPU delta pure and tested, pid reuse keyed by start time.
- Shell.run sequential pipe drain with no timeout. Concurrent drain, 10 s watchdog.
- lsof resolved through PATH in an unsandboxed app. Fixed /usr/sbin/lsof.
- hasNotch predicate differed from DynamicNotchKit's; NSScreen.screens[0] trap; untracked transition tasks; hide() left polling running. Aligned predicate, explicit screen, one serialized transition task, stores stop on hide.
- Copilot missing percent_remaining rendered 100 percent used; has_quota defaulted true. Both now skip.
- gh stderr interpolated into an error that reaches the log. Exit code only.
- Portless: pid required; bad file kept stale routes. Optional pid, cleared on failure, fixture test added.
- lsof failure kept stale servers. Cleared so lastError shows.
- ARDAgent in the server list. Denylisted.
- "Resets now" on stale Codex data. Countdown hidden when the snapshot is over an hour old.
- devHUD.xcodeproj committed alongside project.yml. Ignored.
- cwd read unbounded by MAXPATHLEN. Bounded.
- Claims manifest miscounted and cited a verify file that did not exist. Corrected; this file.

### Rejected, with evidence
- "rusage_info_t rebinding is UB": the kernel writes into the struct's own 296-byte storage; verified status 0 and sane values by both the scratch binary and the Opus probe.
- "CPU time is already nanoseconds, timebase scaling is wrong": measured 1.000 s for a 1 s burn with the scaling in place.
- "Codex unlimited windows skipped wrongly": an unlimited quota has no percentage to draw.

### Accepted gaps
- Codex search caps at the 20 newest files by mtime; a missing JSONL timestamp falls back to now.
- Scoped package names drop the scope; portless subtitle shows hostname plus "port N"; menu says "Toggle HUD".
- Countdown text only refreshes when a store publishes (no TimelineView).
- Compact state, hover expand, and the real notch geometry are untested: this machine has no notch.
- Portless join is unit-tested only: portless is not installed here.
- TASKS.md stage indicators not written (root CLAUDE.md forbids editing it unasked).
- No commit: root CLAUDE.md says never commit unless asked.

## Boundary
No test, fixture, or eval was weakened. Two tests were added (CPUDelta, optional
portless pid). All remediation edits touched App/ sources only.
