# Verify: side pill HUD

TASK: T2   **claims:** .buildloop/claims/T2.md   **date:** 2026-09-04
**RESULT: PASS** (with accepted gaps, listed below)

## 5a Self-check
| Check | Result |
|---|---|
| xcodegen generate | PASS |
| xcodebuild build | PASS, no warnings in App/ |
| xcodebuild test | PASS, 44 tests, 0 failures (32 parsers + 5 PillMetric + 7 HoverResolver) |
| grep DynamicNotchKit in App/, project.yml | 0 |
| live: concealed sliver at the right edge after launch and after close | PASS |
| live: revealed pill, brand marks in 40 pt rings, bold percents, badge, gear, swept ears | PASS |
| live: Claude, Copilot, Codex and Dev Servers cards left of the pill, pointer on the ring, no overlap | PASS |
| RSS / CPU after 25 s | 91.5 MB, 0.1 percent |

Live states were driven through the DEBUG distributed-notification hook and
captured with screencapture. Synthetic mouse input is blocked here (no
Accessibility grant), so hover itself was not exercised end to end; see gaps.

## 5b Evals
Project CLAUDE.md changed (architecture, common mistakes). Canary check passes.

## 5c Formal verification
State machine is concealed / revealed / card with two timers, all on the main
actor, and the cursor is resolved from window frames by a pure function
(`HoverResolver`, 7 tests). Triaged out of TLA+.

## 5d Cross-model doubt
Opus code-reviewer subagent: 19 findings, re-read after the hover rewrite.
`codex exec -m gpt-5.5` on the hover-era code: 9 findings.

### Fixed
- HIGH SwiftUI `onHover` on a never-key panel unproven, and cross-window enter/exit ordering could close the card under the cursor. Replaced per-view hover with global + local mouse-move monitors resolved against window frames (`HoverResolver`), one target at a time.
- HIGH no dismissal path for a debug-opened card. Any mouse move outside both frames now schedules close and conceal; `conceal()` dismisses the card.
- HIGH stale claims. Manifest, coverage and every live check redone against the current tree.
- MEDIUM first-mouse on a non-key panel. `HUDHostingView.acceptsFirstMouse` returns true.
- MEDIUM concealed frame hung 59 pt past the screen edge. The window is now `peek` wide when concealed; the hosting view keeps full size inside a container.
- MEDIUM empty `NSScreen.screens` moved the panel to zero. `pillFrame` is optional and `reposition` keeps the last frame.
- MEDIUM menu popUp ran inside the tap action and the pill concealed under its own menu. Deferred with `DispatchQueue.main.async`, `NSMenuDelegate` sets `menuOpen`, conceal respects it.
- MEDIUM tap-to-toggle was unreachable under hover-open. Rings take no clicks now; spec R5 amended.
- MEDIUM SwiftUI shadow band swallowed clicks along the screen edge. Both panels use `hasShadow = true` and draw no shadow themselves; the hosting view is exactly the pill.
- MEDIUM spec R4 said conceal never happens with a card open. Amended: conceal closes the card first.
- MEDIUM no tests on the interaction surface. `HoverResolverTests` covers rows, gaps, gear, sliver, card precedence.
- LOW clamp order let a tall card start above the screen. Top clamp applied last.
- LOW `conceal -> closeCard -> scheduleConceal` re-entry. `dismissCard` is separate from `closeCard`.
- LOW two hand-rolled menus. MenuBarExtra uses a `Toggle` ("Keep Pill Visible") bound to the controller.
- LOW dead `HUDMetric` tokens contradicting `PillMetric.cardWidth`. Deleted.
- LOW double `rootView` evaluation per hover. Only reassigned when the pointer moves.

### Rejected (codex)
- "Transparent panels still block clicks": AppKit hit-tests non-opaque windows by pixel alpha; fully transparent pixels pass clicks through. The pill hosting view is exactly the pill shape.

### Accepted
- Pointer clamp of 18 pt can meet the 14 pt corner radius in the clamped case (cosmetic; changing it means changing a test expectation, left for Zac).
- Observers are never removed; the controller lives as long as the app.
- Provider cards list every window unbounded; today's providers ship at most five.
- The interaction timers (0.35 s close, 0.8 s conceal) are not under test.
- Hover, gear menu, and click pass-through verified by reading the code and the resolver tests, not by a real mouse. First real-mouse session is Zac's.

## Boundary
No test, fixture, or eval was weakened. Twelve tests were added (PillMetric,
HoverResolver). Remediation edits touched App/ sources and the spec text only.

## Human verification
Zac drove the ~/Applications build with a real mouse on 2026-09-04: reveal on approach, card hover, card buttons, first click, kill, restart, and both menus all confirmed working ("everything is super clean and works").
