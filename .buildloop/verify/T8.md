# Verify: Codenotch parity

TASK: T8   **claims:** .buildloop/claims/T8.md   **date:** 2026-09-05
**RESULT: PASS** (Claude data pending the keychain grant)

| Check | Result |
|---|---|
| build + tests | PASS, 97 tests |
| live sessions | PASS: this Claude Code session listed with name, surface, folder, working duration |
| pin | PASS |
| hide for an hour | PASS |
| bands | PASS: 32 percent Codex ring is green |
| Cursor | no slot, correct for a signed-out Cursor |
| Claude ring | "--" until Zac grants keychain access to devHUD; the fetch blocks on the prompt |

Found on the way: the file token had expired while Claude Code kept the
keychain one fresh (the 401). The test runner hung once because the keychain
read ran at launch on the main thread; it now runs only inside a fetch and
never under XCTest.

Doubt pass deferred: run codex and the Opus reviewer over T7 and T8 together
next session. Nothing from vinzdg/codenotch was copied; its repository has no
license.
