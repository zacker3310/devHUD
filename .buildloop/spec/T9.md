# Spec: motion

**id:** T9   **intent:** .buildloop/intent/T9.md
**status:** accepted

R1. `HUDState.hoveredRow` mirrors the resolver's pill item. The hovered ring scales to 1.1 on a
    spring (response 0.25, damping 0.7) and its label goes full white; other rings drop to 0.7 opacity.
R2. Percent labels and "N% Used" use `contentTransition(.numericText())`.
R3. The card content enters with opacity 0 to 1, scale 0.96 to 1 anchored at the pill side, and an
    8 pt offset from the pill, on a spring (response 0.28, damping 0.8); switching rings crossfades
    the content keyed by selection in 180 ms.
R4. The pill slide uses an ease-out-back curve (control points 0.34, 1.4, 0.64, 1) over 0.32 s.
R5. The gear rotates 40 degrees and scales to 1.08 on hover, springing back on exit.
R6. Server action buttons enter with move-from-trailing plus opacity on a snappy 0.2 s curve.
R7. No animation runs continuously except the existing working arc and waiting pulse, which
    encode live state.
