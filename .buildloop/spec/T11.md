# Spec: watch rows

**id:** T11   **intent:** .buildloop/intent/T11.md
**status:** accepted

R1. `WatchItem { id, title, subtitle, state ok|busy|attention|failed, at, url }`, `WatchSummary` with
    badge = failed + busy + attention and worst state.
R2. `VercelParser.latestPerProject` over `vercel ls --json`: newest deployment per name; READY ok,
    ERROR failed, BUILDING/QUEUED/INITIALIZING busy, else attention; subtitle state · target · first
    commit line; url https://<deployment url>.
R3. `GitHubParser` over gh api: notifications (attention), search/issues review-requested (attention)
    and author:@me (ok), latest actions run per recently pushed owned repo (success ok, failure or
    timed_out failed, other completed attention, running busy).
R4. `VercelStore` runs `vercel ls --json` through `/bin/zsh -lc` (node on PATH), every 120 s, 30 s
    while anything builds; `GitHubStore` runs gh from a fixed path every 120 s. Missing CLI: no row.
R5. Pill rows `.github` and `.vercel` after servers: brand mark in a circle whose stroke is the worst
    state's colour, label the badge count. Cards list items newest first with a state dot, age, and
    an open arrow.

AC1. parsers tested against captured shapes | CHECK:tests
AC2. live: Vercel row shows stagger ready; GitHub row shows the latest runs | CHECK:live
