# Intent: devHUD v1, read-only notch HUD, live on this machine

**id:** T1   **originator:** Zac (TASKS.md, session 2026-09-04)   **date:** 2026-09-04
**status:** accepted

## Problem
Nothing is built. TASKS.md describes a macOS menu-bar utility that puts AI usage
rings (Claude Code, Codex, Copilot) and a dev-server list into the notch. The
ask this session: "implement build and take it live on this machine for testing".

## Desired outcome
A signed-for-local-use `devHUD.app` runs on this Mac mini (Mac16,10, no notch,
1920x1080 external display), shows in the menu bar with no Dock icon, and opens
a DynamicNotchKit floating panel with two cards: AI usage (three providers with
percent used and reset countdown) and Dev Servers (listening localhost ports,
process, project, CPU, memory, uptime). Unit tests cover every parser.

## Who is affected
Zac only. Local test build; no distribution, no notarization.

## Constraints
- macOS 14 minimum, `LSUIElement`, not sandboxed (per TASKS.md decision lock).
- Read-only against every provider. Never write to `~/.claude/.credentials.json`
  or `~/.codex/auth.json`.
- No new accounts, no new auth UI. Copilot rides on `gh auth token`.
- Design tokens from TASKS.md, not the acker palette: the target is Codenotch.
- XcodeGen project, same shape as twothings.

## Open questions
Resolved in SPEC with the readings below because the session is autonomous:
1. Portless mode: read `~/.portless/routes.json` directly (spike: on-disk JSON,
   `hostname`, `port`, `pid`). `portless` is not installed here, so the store
   degrades to empty.
2. Kill / open-terminal: deferred to v1.x. TASKS.md says confirm scope first.
3. Copilot auth: `gh auth token` reuse. Spike confirmed `gh api
   copilot_internal/user` returns `quota_snapshots` with the existing token.
4. Name / bundle id: devHUD, `cloud.acker.devhud`. No icon yet.

## Not doing
- Kill or open-terminal actions. Read-only app.
- Developer ID signing, notarization, Phase 5 polish, pixel validation on a real
  notch (this machine has none).
- Keychain storage (nothing new to store).
- libproc port enumeration (v1 uses lsof per TASKS.md).
