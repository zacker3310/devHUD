# Plan: watch rows

**id:** T11   **spec:** .buildloop/spec/T11.md
**premises checked:** 2026-09-05 main at de8aa7d; vercel 0 notifications, 0 PRs, zckr runs green

1. CREATE `App/Watch/WatchModel.swift`, `App/Watch/WatchStores.swift`, `Tests/WatchParserTests.swift`
2. MODIFY `App/Models/HUDState.swift` (cases, rows), `App/Design/Tokens.swift` (watch colours), `App/HUDController.swift`, `App/Views/SidePillView.swift` (WatchCircle), `App/Views/DetailCardView.swift` (WatchCard)
3. Assets: brand-github, brand-vercel (Simple Icons)

CHECK:tests | xcodebuild test | 0 failures
CHECK:live | select:vercel, select:github on display 2 | rows and cards

## Departures
- Vercel runs through the login shell because the CLI is a node script under ~/.npm-global; the command string is fixed.
- `WatchItem.State.attention` covers both canceled deployments and PRs that need a review.
