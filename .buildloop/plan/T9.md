# Plan: motion

**id:** T9   **spec:** .buildloop/spec/T9.md
**premises checked:** 2026-09-05, branch feat/side-pill-hud at aa9a94b

## File operations
1. MODIFY `App/Models/HUDState.swift` (hoveredRow), `App/HUDController.swift` (set hoveredRow from cursorMoved, slide curve).
2. MODIFY `App/Views/SidePillView.swift` (hover lift, numeric text, gear hover), `App/Views/DetailCardView.swift` (entrance, crossfade, numeric text), `App/Views/DevServersCard.swift` (strip transition).

## Verification
CHECK:build | xcodebuild test | 0 failures
CHECK:live | captures mid-entrance and hovered ring | lift and offset visible

## Departures
- Hover cannot be driven here, so the lift is verified by build and by reading; the card entrance was captured mid-spring.
