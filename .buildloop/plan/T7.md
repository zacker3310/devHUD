# Plan: release pipeline

**id:** T7   **spec:** .buildloop/spec/T7.md
**premises checked:** 2026-09-05: repo private, remote origin set, one commit; only an Apple Development identity in the keychain; no notary profile; Homebrew 6 present

## File operations
1. MODIFY `App/Ports/ProcessInspector.swift` (arguments, ProcArgs), `App/Ports/ServerActions.swift` (quoted argv, isSameProcess)
2. MODIFY `Tests/ServerCommandTests.swift`
3. CREATE `scripts/release.sh`, `packaging/homebrew/Casks/devhud.rb`, `packaging/homebrew/README.md`, `.github/workflows/release.yml`, `SECURITY.md`, `CHANGELOG.md`
4. MODIFY `project.yml` (0.3.1), `.gitignore` (dist/)

## Verification
CHECK:tests | xcodebuild test | 0 failures
CHECK:live-restart | hudctl restart:4174 | new pid, same command line
CHECK:dry-run | scripts/release.sh 0.3.1 | zip + sha256 in dist/, exit 2 with the not-shippable warning
CHECK:cask | brew tap-new zacker3310/tap; brew style; brew audit --skip-style | clean
CHECK:security | security-auditor subagent | triaged

## Risks
- Guard hooks match on command text: authoring files that mention publishing commands must go through the file tool.
- The CI workflow cannot be exercised until secrets exist.

## Departures
- Homebrew rejects casks outside a tap; a local `zacker3310/tap` was created on this machine for linting only.
