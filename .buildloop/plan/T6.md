# Plan: pill placement

**id:** T6   **spec:** .buildloop/spec/T6.md
**premises checked:** 2026-09-04, T5 tree

## File operations
1. MODIFY `App/Models/HUDState.swift` — `PillEdge`, `PillPlacement`, `PillMetric.pillFrame`, `cardX`, `isGear`, `HUDState.placement`.
2. MODIFY `App/Views/SidePillView.swift` — mirrored shape for the left edge.
3. MODIFY `App/Views/DetailCardView.swift` — pointer edge.
4. MODIFY `App/HUDController.swift` — placement-driven frames, drag monitors, snap, menu items, hook (`dock:`, `anchor:`, `screen:`).
5. MODIFY `App/DevHUDApp.swift` — Dock and Display menus.
6. CREATE `Tests/PillPlacementTests.swift`.

## Verification
CHECK:build | xcodebuild build | BUILD SUCCEEDED
CHECK:tests | xcodebuild test | 0 failures
CHECK:live-left | hudctl dock:left; select:codex; screenshot | mirrored pill at x=0, card to its right
CHECK:live-anchor | hudctl anchor:0.2; screenshot | pill low on the edge
CHECK:live-persist | relaunch; screenshot | same spot

## Risks
- Drag on a hover-driven pill: the down event also reaches SwiftUI; only the gear takes taps so nothing else fires.
- Multi-display snapping cannot be exercised here (one display).

## Departures
- Mid-task Zac asked for the gear to leave the pill: it is now a 34 pt black
  circle in its own panel under the pill, shown on hover and while the menu is
  open, and a hover target of its own. The pill lost its gear row.
- `NSScreen.main` moved the pill onto the second Dell display (this machine
  has two 1920x1080 displays side by side). The default is now
  `NSScreen.screens.first`, the display with the menu bar.
- Frames clamp to `visibleFrame` with room reserved for the gear, so neither
  the pill nor the gear lands on the Dock or the menu bar.
