# Plan: Docker containers

**id:** T10   **spec:** .buildloop/spec/T10.md
**premises checked:** 2026-09-05, main at cd2ffb4; Docker Desktop 29.5.3 running

1. CREATE `App/Docker/DockerModel.swift` (DockerContainer, PortMapping, DockerPS, DockerStats)
2. CREATE `App/Docker/DockerStore.swift` (polls, probe cache, stop/restart actions)
3. MODIFY `App/Views/DevServersCard.swift` (Containers section), `App/Views/SidePillView.swift` (badge count), `App/Views/DetailCardView.swift`, `App/HUDController.swift` (store)
4. CREATE `Tests/DockerParserTests.swift`

CHECK:tests | xcodebuild test | 0 failures
CHECK:live | select:servers on display 2 | containers listed with ports and stats

## Departures
- Containers sort by compose project then name, so unprojected ones come first; a test picked index zero and had to select by name.
- Stop and restart live on DockerStore rather than ServerActions: they are docker CLI calls, not signals.
