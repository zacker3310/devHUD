# Plan: launch at login

**id:** T5   **spec:** .buildloop/spec/T5.md
**premises checked:** 2026-09-04, T4 tree

## File operations
1. CREATE `App/LoginItem.swift`
2. MODIFY `App/Models/HUDState.swift` (launchAtLogin), `App/HUDController.swift` (set/refresh, menu item, hook), `App/DevHUDApp.swift` (Toggle)
3. CREATE `scripts/install.sh`

## Verification
CHECK:build | xcodebuild build | BUILD SUCCEEDED
CHECK:install | scripts/install.sh | /Applications/devHUD.app running
CHECK:live-login | hudctl login:on; app log | "login item: enabled"
CHECK:live-logoff | hudctl login:off; app log | "login item: not registered"

## Risks
- Registering the DerivedData copy would pin a path that changes; the install script exists for that reason.
- macOS may report requiresApproval on first registration for an unsigned-by-team app.

## Departures
- `scripts/install.sh` takes `CONFIGURATION` (default Release) so a Debug copy
  can be installed to drive the hook; the Release copy replaced it at the same
  path afterwards, which keeps the registration.
- First status from a fresh copy reads "not found" until the first register.
