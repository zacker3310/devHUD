# Plan: T3

**id:** T3   **spec:** .buildloop/spec/T3.md
**premises checked:** 2026-09-04, T2 tree

## File operations
1. MODIFY `App/Models/Usage.swift` — add `ProviderSlot`.
2. MODIFY `App/Providers/UsageProvider.swift` — `slot` replaces `id`.
3. MODIFY `App/Providers/ClaudeUsageProvider.swift` — `limitWindow`, `init(configDir:)`, `discoverConfigDirs`, title from subscriptionType.
4. MODIFY `App/Providers/CodexUsageProvider.swift`, `CopilotUsageProvider.swift` — slots.
5. MODIFY `App/Stores/AIUsageStore.swift` — keyed by slot id, `slots`.
6. MODIFY `App/Models/HUDState.swift` — `rows`, `HUDSelection.provider(ProviderSlot)`, geometry by row count, resolver takes rows.
7. MODIFY `App/Ports/DevServer.swift` — `ServerKind`, `kind`, `url`, `ServerKind.known`.
8. CREATE `App/Ports/ServerProbe.swift` — `classify`, `probe(port:)`.
9. MODIFY `App/Stores/PortsStore.swift` — kind cache and probe scheduling.
10. MODIFY `App/Views/SidePillView.swift`, `DetailCardView.swift`, `DevServersCard.swift` — rows, slot titles, chips, click.
11. MODIFY `App/HUDController.swift` — rows from slots, card hosted in `HUDHostingView`, measured by a throwaway `NSHostingController`.
12. MODIFY tests for the new signatures; CREATE `Tests/ServerKindTests.swift`, `Tests/ClaudeDiscoveryTests.swift`.

## Verification
CHECK:build | xcodebuild build | BUILD SUCCEEDED
CHECK:tests | xcodebuild test | 0 failures
CHECK:live-fable | hudctl select:claude; fast screenshot | "7d Fable" row
CHECK:live-kinds | python3 -m http.server 4173; hudctl select:servers; screenshot | chip "web" on 4173
CHECK:discovery | unit test | second dir with credentials yields second slot

## Risks
- Probing a port that speaks a fragile protocol (some dev DB proxies drop on garbage). Mitigated by known-command mapping first.
- Screenshots race real mouse movement now that hover owns the state; capture within 0.3 s.

## Departures
- `HUDSelection.provider` carries a `ProviderSlot`, and `HUDState.rows` is the
  single source of pill order; `PillMetric` functions take the row list or count.
- The card is displayed by `HUDHostingView` (first-mouse aware) and measured by
  a throwaway `NSHostingController`, since the view reports no intrinsic size.
- Two instances overlapped once during testing after a relaunch; the second
  capture showed a shrunken card that did not reproduce with a single instance.
- Ring order for two Claude accounts follows discovery order (default dir first).
