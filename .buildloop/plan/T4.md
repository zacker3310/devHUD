# Plan: server actions

**id:** T4   **spec:** .buildloop/spec/T4.md
**premises checked:** 2026-09-04, T3 tree

## File operations
1. CREATE `App/Ports/ServerActions.swift` — `ServerCommand` (pure), `PendingAction`, `ServerActions`.
2. MODIFY `App/Views/DevServersCard.swift` — hover action strip, confirm row, in-flight and failed subtitles.
3. MODIFY `App/Views/DetailCardView.swift`, `App/HUDController.swift` — pass `ServerActions`; DEBUG hook gains `askkill:`, `kill:`, `restart:`.
4. CREATE `Tests/ServerCommandTests.swift`.

## Verification
CHECK:build | xcodebuild build | BUILD SUCCEEDED
CHECK:tests | xcodebuild test | 0 failures
CHECK:live-kill | http.server on 4173; hudctl kill:4173; lsof | no listener on 4173
CHECK:live-restart | http.server on 4174; hudctl restart:4174; lsof | a different pid on 4174, log file written

## Risks
- Restarting relaunches the listener process's argv, not its wrapper (pnpm dev -> node child).
- A process ignoring SIGTERM gets SIGKILL after 3 s; data loss is the user's call at the confirm.

## Departures
- `.help` tooltips on the icon buttons; `ProgressView` while an action is in flight.
- The relaunched process is a child of the login shell, not of devHUD; it survives devHUD quitting.
