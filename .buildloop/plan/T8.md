# Plan: Codenotch parity

**id:** T8   **spec:** .buildloop/spec/T8.md
**premises checked:** 2026-09-05, branch feat/side-pill-hud clean

## File operations
1. MODIFY `App/Models/Usage.swift` (ProviderID.cursor, UsageBand), `App/Design/Tokens.swift` (band colours), `App/Views/UsageFormat.swift` (resetText), `App/Views/UsageRing.swift` (band colour, activity overlay, dimming).
2. CREATE `App/Sessions/AgentSession.swift` (AgentSession, ClaudeSessionRecord, ActivitySummary), `App/Sessions/ActivityStore.swift` (monitors).
3. CREATE `App/Providers/CursorUsageProvider.swift` (CursorUsageDecoder, SQLite credentials).
4. MODIFY `App/Stores/AIUsageStore.swift` (adaptive interval), `App/Views/DetailCardView.swift` (sessions, stale header), `App/Views/SidePillView.swift` (activity per ring), `App/HUDController.swift` (pin, hide for an hour, right-click, monitors), `App/DevHUDApp.swift` (Hide for 1 Hour).
5. CREATE tests: `UsageBandTests`, `ClaudeSessionRecordTests`, `CursorUsageDecoderTests`; MODIFY `FormatAndBackoffTests` (reset copy).
6. Assets: `brand-cursor` (Simple Icons, CC0).

## Verification
CHECK:tests | xcodebuild test | 0 failures
CHECK:live-activity | Debug build; select:claude | working arc on the Claude ring, session listed
CHECK:live-cursor | Debug build | Cursor ring present or absent per token
CHECK:live-pin | hudctl pin:claude; move mouse away; card stays | pinned
CHECK:live-hide | hudctl hidehour; reveal ignored | hidden

## Risks
- Cursor's usage-summary shape is inferred from a third-party description, not documented; the decoder is tolerant and the slot degrades to "--".
- Cursor may hold state.vscdb open; read-only open handles WAL, a locked DB yields no slot until next poll.

## Departures
- Mid-task the Claude ring went 401: the file token had expired at 03:02 while
  Claude Code kept refreshing the one in the login keychain ("Claude
  Code-credentials"). The provider now reads the keychain first and the file
  second, refuses an expired token with a message, and says "sign in again"
  on 401. The keychain read is skipped under XCTest and never happens at
  launch on the main thread: its access prompt is modal and hung the test
  runner once.
- Cursor is installed here (state store present) though not in /Applications;
  the slot appears only when the store yields a session token.
- Click-to-pin is wired through the existing drag monitor: a mouse-up without
  movement on a ring toggles the pin.
