CLAIMS::v2
TASK: T7          PLAN: .buildloop/plan/T7.md

== DELTA_MANIFEST ==
FILES_CREATED: 6
  scripts/release.sh | CREATE | version stamp, tests, Developer ID + hardened runtime build, ditto zip, notarize + staple, sha256, cask update, guarded tag |
  packaging/homebrew/Casks/devhud.rb | CREATE | cask | sonoma+, login item uninstall, zap
  packaging/homebrew/README.md | CREATE | tap and non-admin install notes |
  .github/workflows/release.yml | CREATE | tag-triggered build, notarize, publish |
  SECURITY.md | CREATE | reads, sends, executes, writes, sandbox, reporting |
  CHANGELOG.md | CREATE | 0.3.1 notes |
FILES_MODIFIED: 5
  App/Ports/ProcessInspector.swift | arguments(pid:) via KERN_PROCARGS2, ProcArgs keeps the exec path | injection fix
  App/Ports/ServerActions.swift | relaunch runs the exec path with quoted args; terminate re-checks identity each tick; safeLogName | injection + pid reuse + log path
  App/Ports/LsofParser.swift, ServerProbe.swift, Portless/PortlessRoutes.swift, DevServer.swift, Stores/PortsStore.swift, Providers/UsageProvider.swift | pid guard, no redirects, trusted hostnames, URLComponents, fixed gh paths, SIGKILL watchdog | security review
  Tests/ServerCommandTests.swift | hostile argv, ProcArgs, own argv | 4 tests changed/added
  project.yml | 0.3.1 | n/a
  .gitignore | dist/ | n/a

== SPEC_COVERAGE ==
CRITERION:R1 exact quoted argv | ProcessInspector.arguments, relaunchScript | tests + live restart
CRITERION:R2 pid reuse guard | isSameProcess | code + tests (own pid)
CRITERION:R3 release script | scripts/release.sh | dry run: zip + sha, exit 2 unsigned
CRITERION:R4 cask lints | brew style + audit in local tap | see matrix
CRITERION:R5 workflow | .github/workflows/release.yml | not runnable without secrets
CRITERION:R6 SECURITY.md | file | review
CRITERION:R7 security review | security-auditor subagent | see verify

== KNOWN_GAPS ==
GAP:no-devid | keychain holds only "Apple Development"; release is ad-hoc until Zac creates a Developer ID Application certificate | Zac
GAP:no-notary | no devhud-notary keychain profile | Zac
GAP:private-repo | Homebrew cannot fetch from a private release | Zac decides visibility or a public tap + public release repo
GAP:ci-unrun | workflow untested without secrets | first tag push
GAP:no-commit | nothing committed, so nothing tagged | Zac asked for a tagged release; awaiting go for commit + push

== ORDERING_SURFACE ==
SURFACE:none

== VERIFICATION_MATRIX ==
CHECK:tests | xcodebuild test | 76 tests, 0 failures | PASS
CHECK:live-restart | hudctl restart:4174 | pid 72512 -> 72537, same command line | PASS
CHECK:dry-run | scripts/release.sh 0.3.1 | dist/devHUD-0.3.1.zip + .sha256, cask sha updated, exit 2 | PASS
CHECK:cask | brew style --cask; brew audit --skip-style (local tap) | no offenses, no audit output | PASS
CHECK:security | security-auditor | 10 findings: 9 fixed, 1 blocked on credentials | PASS
