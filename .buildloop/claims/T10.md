CLAIMS::v2
TASK: T10         PLAN: .buildloop/plan/T10.md

== DELTA_MANIFEST ==
FILES_CREATED: 3
  App/Docker/DockerModel.swift | CREATE | DockerContainer, PortMapping, DockerPS.parse/parsePorts/parseLabels, DockerStats.parse |
  App/Docker/DockerStore.swift | CREATE | list every 5 s, stats every 15 s, host-port probes, stop/restart with confirm |
  Tests/DockerParserTests.swift | CREATE | 5 tests from shapes captured on docker 29.5.3 |
FILES_MODIFIED: 4
  App/HUDController.swift | owns DockerStore | n/a
  App/Views/SidePillView.swift | badge counts listeners plus containers | n/a
  App/Views/DetailCardView.swift, DevServersCard.swift | Containers section: name, project chip, image, ports, health, cpu, memory; restart and stop; click opens a web port | n/a

== SPEC_COVERAGE ==
CRITERION:R1 model and ps parse | DockerPS | tests
CRITERION:R2 stats parse | DockerStats | tests
CRITERION:R3 binary discovery, daemon-down state | DockerStore | live (Docker Desktop running)
CRITERION:R4 port probes | scheduleProbe | live (web chips where a port answers HTTP)
CRITERION:R5 rows and actions | ContainerRow | live capture; stop and restart not exercised on Zac's Supabase stack on purpose

== KNOWN_GAPS ==
GAP:actions-unexercised | stop and restart were not run against live containers | they are single docker CLI calls; the row confirms stop
GAP:daemon-down | the Docker-not-running line was not captured | code path: non-zero exit clears the list

== ORDERING_SURFACE ==
SURFACE:none

== VERIFICATION_MATRIX ==
CHECK:tests | xcodebuild test | 102 tests, 0 failures | PASS
CHECK:live | select:servers on display 2 | Supabase containers listed | see verify
